export default class Community {
  constructor(width, height, context, numModels) {
    this.numModels = numModels;
    this.communities = {};
    this.height = height;
    this.widht = width;
    this.context = context;
  }

  // setup method (initializes models)
  setup() {
    // initialize all models
  }

  // relocation method

  //
}
